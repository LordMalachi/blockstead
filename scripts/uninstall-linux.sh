#!/usr/bin/env bash
set -euo pipefail
echo "The milestone preview installer makes no changes, so there is nothing to uninstall."
echo "Future uninstall behavior will preserve /var/lib/blockstead and all Minecraft folders by default."
