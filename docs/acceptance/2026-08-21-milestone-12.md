# Milestone 12 acceptance record — 2026-08-21

## Result

**In progress.** This run exercised the CI-equivalent checks available in the
workspace, but it was not performed on Linux Mint 22.3. It therefore does not
close Milestone 12, and no checkbox in the Mint release checklist is represented
as complete. A clean Mint 22.3 run must repeat the automated commands and every
manual checklist and Milestone 12 scenario before release closeout.

## Environment

| Item | Observed value |
| --- | --- |
| Date (UTC) | 2026-08-21 |
| Operating system | Ubuntu 24.04.4 LTS (not authoritative for Mint acceptance) |
| Architecture | x86_64 |
| Kernel | Linux 6.18.35 |
| Source tree tested | Base `d9a00275d6a84536169037c1805e6a0bcd023716` plus the full PR #12 change set, subsequently committed as `ae813b9151c687cd156fdd2fd95e805db8348fa7` |
| Python | 3.12.13 (the exact repository request, 3.12.8, was unavailable) |
| Node / npm | 20.20.2 / 11.4.2 |
| Java | OpenJDK 21.0.2 |
| Browser | Not installed; Playwright download was denied by the environment proxy |
| Tester | Automated workspace agent |

## Commands and observations

| Command | Result | Observation |
| --- | --- | --- |
| `.venv/bin/python -m pytest backend relay` | Pass | 617 passed, 3 skipped; real-loader tests and one frontend-dependent SPA test were skipped. |
| `.venv/bin/ruff check backend relay` | Pass | No findings. |
| `.venv/bin/mypy backend/src` | Pass | 56 source files checked. |
| `.venv/bin/mypy --config-file relay/pyproject.toml relay/src` | Pass | 7 source files checked. |
| `.venv/bin/pip-audit` | Pass after environment repair | The freshly created virtual environment initially contained a vulnerable bootstrap `pip`; upgrading it to 26.2.1 produced no known dependency vulnerabilities. Local editable packages are not published on PyPI and were skipped. |
| `npm ci --prefix frontend` | Pass with audit finding | The locked tree installed reproducibly; npm initially reported four high-severity findings. |
| `npm audit --prefix frontend --audit-level=high` | Pass after targeted lock update | Updated React Router to 7.18.2 and patched transitive `js-yaml` and `nanoid`; the final audit reported zero vulnerabilities. |
| `npm test --prefix frontend` | Pass | 140 tests passed; existing React `act()` warnings were emitted by two maintenance tests. |
| `npm run lint --prefix frontend` | Pass | No findings. |
| `npm run build --prefix frontend` | Pass with warning | Production build completed; Vite reported a chunk larger than 500 kB. |
| `shellcheck scripts/*.sh tests/integration/*.sh packaging/bin/blockstead packaging/blockstead-power packaging/blockstead-update` | Pass | No findings. |
| `desktop-file-validate packaging/desktop/blockstead.desktop "Install Blockstead.desktop"` | Pass | Both launchers validated. |
| `tests/integration/service-start-program-test.sh` | Pass | Staged-tree service start-program exchange passed. |
| `tests/integration/run-native-update-tests.sh` | Not run | Docker is unavailable in this workspace. This remains a required gate. |
| `npx --prefix frontend playwright install chromium` | Blocked | The environment proxy returned HTTP 403 for the Playwright CDN, so browser tests could not run. This remains a required gate. |

## Manual acceptance and redacted evidence

No fresh install, upgrade, rollback, reboot, uninstall, system service, PolicyKit,
permissions, disposable-profile, account-recovery, or live Discord-webhook
scenario was performed because this host is not Linux Mint 22.3 and no disposable
Discord endpoint was provided. Consequently there is no manual evidence to
attach. This record intentionally contains no webhook value, credential, player
address, or private world path.

## Required follow-up

1. Check out the closeout revision on a clean x86_64 Linux Mint 22.3 host and
   record its edition, kernel, Python, Java, browser, tester, and revision.
2. Repeat every automated command above, including Playwright and the native
   updater container test.
3. Complete every checkbox in `docs/linux-mint-release-checklist.md`, including
   all saved-setup, viewer-access/recovery, and Discord-alert scenarios.
4. Attach redacted observations and evidence to this dated record (or a new
   record for the actual run). If every gate passes, only then mark Milestone 12
   Complete and add its release closeout to the changelog.
